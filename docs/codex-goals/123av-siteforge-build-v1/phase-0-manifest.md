# Phase 0 Manifest

## 范围冻结

本阶段冻结 `https://123av.com/zh/dm9` 的接入目标、公开只读边界、评分方式和完成证据要求。

## 基线状态

- 当前分支：`main`。
- worktree 在任务开始前已有未提交变更，必须保留。
- `123av.com` 在任务开始时未出现在仓库配置或适配器列表中。
- 目标站点类型人工标注为 `catalog-detail`。
- 需要新增 `123av` 专属 SiteAdapter。

## 已观察结构证据

- 请求 URL 可公开返回 HTML。
- 采样结构包含本地化目录页、目录排序页、影片详情页模板、标签页、演员索引、搜索表单和合规页面。
- 未观察到可验证公开 API 信号；程序接口能力必须保持未激活或拒绝。

## 安全边界

- 仅限公开只读结构采集。
- 不登录、不使用 cookie、不绕过访问控制、不下载、不解析媒体资源。
- 不执行写操作、发布、删除、支付、账号变更或私信。
- 报告、skill 和能力字段不得包含敏感页面材料。

## Phase 0 Gate

Phase 0 通过条件：

- 目标 ledger 存在。
- 三层评分与硬性封顶规则已记录。
- 站点类型、适配器要求和安全边界已记录。
- 后续实现必须按本 manifest 执行。

状态：complete.
